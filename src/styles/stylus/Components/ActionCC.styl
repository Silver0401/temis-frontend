.ActionCC
    position: relative
    fillSpace()
    alignStuff(0,1,0,1)
    justify-content: center
    border: 0.5px solid var(--white)
    border-radius: 15px

    .emptyIconContainer
        position: relative
        width: 96px
        height: 96px
        alignStuff(0,1,0,0)
        opacity: 0.5

        svg
            width: 100%
            height: 100%

    .textP
        margin: 0px 30px
        text-align: center
        margin-top: 20px
        margin-bottom: 25px
        position: relative
        font-size: 0.9rem
        color: var(--white)

    .AddNewDataButton
        position: relative
    